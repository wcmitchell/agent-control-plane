apiVersion: v1
kind: Service
metadata:
  name: mock-llm
  namespace: ambient-code
  labels:
    app: mock-llm
spec:
  type: ClusterIP
  selector:
    app: mock-llm
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
